Scaffold-DbContext "Server=.;Database=dotnetcore-adventure;Integrated Security=True;" Microsoft.EntityFrameworkCore.SqlServer -OutputDir Database -Context SqlAdventureContext -DataAnnotations -Force

After scaffolding:
- Adapt the generated SqlAdventureContext (remove the override)
- Adapt ErrorLog:
       [Column(TypeName = "nvarchar(4000)")]
       public string ErrorMessage { get; set; }
